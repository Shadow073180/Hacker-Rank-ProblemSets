object Solution {

    // Complete the compareTriplets function below.
    def compareTriplets(a: Array[Int], b: Array[Int]): Array[Int] = {
        var alicePoints = 0
        var bobsPoints = 0
        var iteration = 0
        var answer: Array[Int]= new Array[Int](2)
        var answer = (alicePoints,bobsPoints) 
        
        for (i <- a )
            println(i)
        return answer

    }
}